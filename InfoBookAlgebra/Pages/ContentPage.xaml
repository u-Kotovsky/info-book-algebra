<Page x:Class="InfoBookAlgebra.Pages.ContentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:InfoBookAlgebra.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{Binding CurrentTheme.Name}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Label x:Name="TitleLabel" FontSize="24" FontStyle="Normal" 
                   Content="{Binding Path=Title, Mode=OneWay}"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBox x:Name="ContentTextBox" IsReadOnly="True"
                     TextWrapping="WrapWithOverflow" VerticalScrollBarVisibility="Visible"
                     Text="{Binding Path=Content, Mode=OneWay}"></TextBox>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding BackCommand}">Back</Button>
        </Grid>
    </Grid>
</Page>
